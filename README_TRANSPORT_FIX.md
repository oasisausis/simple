# Transport panel fix

Replace `styles.css` and the HTML pages if using the new navigation.

Main correction: `.span-12 { grid-column: 1 / -1; }`. The missing rule caused the onboarding panel to collapse into a narrow strip.

Also added **Getting here** to the main navigation, footer, and Campus tab bar.
